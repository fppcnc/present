In folder "sql", run the entirety of dbStructure.sql to build the database.

It´s already possible to create new rows in database just by using the website.
In case it´s needed, file populateDb.sql will add some rows to the already created Db.

Have fun!